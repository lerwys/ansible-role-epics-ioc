---
__epics_ioc_packages:
  - "git{{ (epics_ioc_pkg_version_debian_git | length > 0) | ternary('=' + epics_ioc_pkg_version_debian_git, '') }}"
  - "make{{ (epics_ioc_pkg_version_debian_make | length > 0) | ternary('=' + epics_ioc_pkg_version_debian_make, '') }}"
